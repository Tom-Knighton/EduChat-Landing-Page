using System;
using System.Security.Cryptography;
using System.Text;

namespace IDentityTestsConsole
{
    class MainClass
    {
        public static void Main(string[] args)
        {
            Console.WriteLine(Guid.NewGuid()); Console.WriteLine(Guid.NewGuid());
            Console.WriteLine(Guid.NewGuid());
            Console.WriteLine(Guid.NewGuid());
        }
    }
}
